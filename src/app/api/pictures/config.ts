export enum ERROR_STATUS {
  UNAUTHORIZED = 401,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  INTERNAL_SERVER_ERROR = 500
}

export enum SUCCESS_STATUS {
  OK = 200,
  CREATED = 201,
  NO_CONTENT = 204,
}
